{
    "configurations": [
        {
            "name": "C/C++: g++-13 Générer et déboguer le fichier actif",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang++ générer le fichier actif"
        }
    ],
    "version": "2.0.0"
}